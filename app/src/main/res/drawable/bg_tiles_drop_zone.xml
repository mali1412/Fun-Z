<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="@dimen/drop_zone_radius" />
    <stroke
        android:width="@dimen/tile_stroke_width_bold"
        android:color="@color/tiles_drop_border"
        android:dashWidth="@dimen/drop_zone_dash_width"
        android:dashGap="@dimen/drop_zone_dash_gap" />
    <solid android:color="@color/tiles_drop_bg" />
</shape>
