<?xml version="1.0" encoding="utf-8"?>
<!-- bottom_sheet_hint.xml — BottomSheet de Pista -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:orientation="vertical" android:padding="16dp"
    android:background="?attr/backgroundColor">

    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="horizontal" android:gravity="center_vertical"
        android:layout_marginBottom="12dp">

        <TextView android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:text="@string/btn_hint" android:textSize="15sp"
            android:textStyle="bold" android:textColor="@color/text_primary"/>

        <Button android:id="@+id/btn_close_hint" android:layout_width="32dp" android:layout_height="32dp"
            android:text="@string/btn_exit" android:textSize="14sp" android:padding="0dp"
            style="@style/Widget.Material3.Button.TextButton"
            android:textColor="@color/text_secondary"/>
    </LinearLayout>

    <TextView android:id="@+id/tv_hint_content" android:layout_width="match_parent"
        android:layout_height="wrap_content" android:text="@string/load_hint"
        android:textSize="13sp" android:textColor="@color/text_primary"
        android:lineSpacingExtra="5dp" android:layout_marginBottom="8dp"/>
</LinearLayout>
