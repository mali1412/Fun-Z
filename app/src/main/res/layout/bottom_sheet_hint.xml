<?xml version="1.0" encoding="utf-8"?>
<!-- bottom_sheet_hint.xml — BottomSheet de Pista -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:orientation="vertical" android:padding="@dimen/margin_default"
    android:background="?attr/backgroundColor">

    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="horizontal" android:gravity="center_vertical"
        android:layout_marginBottom="@dimen/margin_medium">

        <TextView android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:text="@string/btn_hint" android:textSize="@dimen/text_size_subtitle"
            android:textStyle="bold" android:textColor="@color/text_primary"/>

        <Button android:id="@+id/btn_close_hint" android:layout_width="@dimen/button_height_tiny" android:layout_height="@dimen/button_height_tiny"
            android:text="@string/btn_exit" android:textSize="@dimen/text_size_small" android:padding="0dp"
            style="@style/Widget.Material3.Button.TextButton"
            android:textColor="@color/text_secondary"/>
    </LinearLayout>

    <TextView android:id="@+id/tv_hint_content" android:layout_width="match_parent"
        android:layout_height="wrap_content" android:text="@string/load_hint"
        android:textSize="@dimen/text_size_caption" android:textColor="@color/text_primary"
        android:lineSpacingExtra="@dimen/margin_tiny" android:layout_marginBottom="@dimen/margin_small"/>
</LinearLayout>
