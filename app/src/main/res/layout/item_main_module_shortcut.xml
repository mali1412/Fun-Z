<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_module_shortcut"
    android:layout_width="@dimen/quick_access_card_width"
    android:layout_height="wrap_content"
    android:layout_marginEnd="@dimen/margin_medium"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="@dimen/card_elevation_small"
    app:strokeWidth="0dp"
    app:cardBackgroundColor="?attr/colorSurface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/margin_medium"
        android:gravity="center">

        <TextView
            android:id="@+id/tv_shortcut_icon"
            android:layout_width="@dimen/shortcut_avatar_size"
            android:layout_height="@dimen/shortcut_avatar_size"
            android:background="@drawable/bg_avatar_circle"
            android:backgroundTint="@color/color_primary_alpha"
            android:gravity="center"
            android:text="@string/shortcut_icon_placeholder"
            android:textColor="@color/color_primary"
            android:textStyle="bold"
            android:textSize="@dimen/text_size_title" />

        <TextView
            android:id="@+id/tv_shortcut_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_small"
            android:text="@string/shortcut_module_placeholder"
            android:textSize="@dimen/text_size_button_small"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:gravity="center"
            android:maxLines="1"
            android:ellipsize="end"/>

        <ProgressBar
            android:id="@+id/pb_shortcut"
            style="@style/Widget.AppCompat.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="@dimen/margin_tiny"
            android:layout_marginTop="@dimen/margin_small"
            android:progress="30"
            android:progressTint="@color/color_primary" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>